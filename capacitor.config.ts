import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GestionCommerciale-PWA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
